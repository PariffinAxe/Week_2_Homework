class Song:
    # _all_songs = []

    def __init__(self, name, artist, genre, length):
        self.name = name
        self.artist = artist
        self.genre = genre
        self.length = length
        # _all_songs.append(self)
